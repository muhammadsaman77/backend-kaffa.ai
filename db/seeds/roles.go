package seeds

func SeedRoles() {
	// This function seeds the roles table with initial data.
	// You can add predefined roles like 'admin', 'user', etc.
	// Example:
	// INSERT INTO roles (name, description) VALUES ('admin', 'Administrator role with full access');
	// INSERT INTO roles (name, description) VALUES ('user', 'Regular user role with limited access');
}

// You can call this function in your main application or migration scripts to ensure roles are seeded.
